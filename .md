| 发布形态 | 系统要求 | 说明 |
| --- | --- | --- |
| Framework（框架依赖） | .NET 8 Runtime + Windows App SDK >= 2.0.1 | 压缩包体积更小，需目标设备预先安装对应的运行时环境 |
| SelfContained（自包含） | 无额外要求 | 内置 .NET 运行时与 Windows App SDK，无需配置环境，开箱即用 |

## 本次更新
1. 现在导出 CSV 会有分类、账户、密码、备注四项，多分类以" &n "分割，多行注释以" \n "换行。
2. 现在允许导出未加密的 CSV、完整数据目录，已加密的 CSV、完整数据目录。
3. 全面重构 UI，现在不再使用大量的对话框，采用自定义的标题栏，让更多空间用于显示内容。